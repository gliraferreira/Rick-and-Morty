<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="viewState"
            type="br.com.lira.rickandmorty.features.characterslist.presentation.viewmodel.CharactersViewState" />

        <variable
            name="toolbarHandler"
            type="br.com.lira.rickandmorty.main.presentation.CommonToolbarHandler" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <include
            android:id="@+id/toolbarView"
            layout="@layout/common_toolbar"
            app:handler="@{ toolbarHandler }"
            app:isSearchEnabled="@{ true }"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:present="@{ viewState.isToolbarVisible }"
            app:title="@{ @string/characters_title }" />

        <include
            android:id="@+id/searchView"
            layout="@layout/characters_search"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:present="@{ viewState.isSearchEnabled }"
            app:isClearButtonVisible="@{ viewState.isSearchClearTextVisible }"/>

        <androidx.constraintlayout.widget.Barrier
            android:id="@+id/verticalBarrier"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:barrierDirection="bottom"
            app:constraint_referenced_ids="toolbarView,searchView" />

        <include
            android:id="@+id/loading"
            layout="@layout/loading_view"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:isVisible="@{ viewState.isLoading }"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rvCharacters"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/verticalBarrier"
            app:present="@{ viewState.shouldDisplayContent }"
            tools:listitem="@layout/list_item_character" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>